# 关系数据库SQL实操语句
以下SQL语句完全对应PDF讲义的章节、页码和示例，覆盖**表定义、基础查询、高级查询、数据库修改**全核心知识点，同时提供Python+SQLite3的串联执行代码，可直接运行实操。
所有示例基于讲义中的`instructor`/`student`/`course`/`takes`/`section`/`teaches`核心表。

## 说明
1. 标注`Pxx`为对应PDF页码；
2. SQL基于**SQLite3**（讲义P39推荐工具），兼容MariaDB/PostgreSQL；
3. Python代码可一键创建表、插入测试数据、执行所有SQL查询。

---

'''shell
sqlite3 "univdb-sqlite.db"
'''


# 一、基础环境准备（Python+SQLite3串联）
**P39-P40**：SQLite3命令行/编程接口使用，以下Python代码可直接运行，创建数据库并初始化所有表，后续SQL均基于此执行。
```python
import sqlite3

# 1. te_sql(sql, desc=""):
    print(f"\n===== {desc} =====")
    try:
        if sql.strip().startswith("SELECT"):
            cursor.execute(sql)
            res = cursor.fetchall()
            for row in res:
                print(row)
        else:
            cursor.execute(sql)
            conn.commit()
            print("执行成功")
    except Exception as e:
        print(f"执行失败: {e}")

# 后续所有SQL语句均通过execute_sql执行，示例：execute_sql("SELECT * FROM instructor", "查询所有讲师")连接SQLite3数据库
db_file = 'univdb-sqlite.db'
conn = sqlite3.connect(db_file)
cursor = conn.cursor()
print(f"连接数据库 {db_file} 成功...")

# 2. 执行SQL语句的工具函数
def execu
```

---

# 二、表定义（DDL）- P41-P44
**核心**：`CREATE TABLE`定义表结构+数据类型+完整性约束（主键/外键/NOT NULL），对应讲义P41-P44的表结构定义。
### 1. 系部表（department）- 外键关联基础表
```sql
CREATE TABLE department (
    dept_name VARCHAR(20) PRIMARY KEY,
    building VARCHAR(20),
    budget NUMERIC(10,2)
);
```
### 2. 讲师表（instructor）- P41-P42 核心示例表
```sql
CREATE TABLE instructor (
    ID CHAR(5) PRIMARY KEY,
    name VARCHAR(20) NOT NULL,
    dept_name VARCHAR(20),
    salary NUMERIC(8,2),
    FOREIGN KEY (dept_name) REFERENCES department(dept_name)
);
```
### 3. 学生表（student）- P43
```sql
CREATE TABLE student (
    ID CHAR(5) PRIMARY KEY,
    name VARCHAR(20) NOT NULL,
    dept_name VARCHAR(20),
    tot_cred NUMERIC(3,0),
    FOREIGN KEY (dept_name) REFERENCES department(dept_name)
);
```
### 4. 课程表（course）- P44
```sql
CREATE TABLE course (
    course_id VARCHAR(8) PRIMARY KEY,
    title VARCHAR(50) NOT NULL,
    dept_name VARCHAR(20),
    credits NUMERIC(2,0),
    FOREIGN KEY (dept_name) REFERENCES department(dept_name)
);
```
### 5. 授课表（teaches）- P20-P23 联表查询核心表
```sql
CREATE TABLE teaches (
    ID CHAR(5),
    course_id VARCHAR(8),
    sec_id VARCHAR(8),
    semester VARCHAR(8),
    year NUMERIC(4,0),
    PRIMARY KEY (ID, course_id, sec_id, semester, year),
    FOREIGN KEY (ID) REFERENCES instructor(ID),
    FOREIGN KEY (course_id) REFERENCES course(course_id)
);
```
### 6. 选课表（takes）- P43
```sql
CREATE TABLE takes (
    ID CHAR(5),
    course_id VARCHAR(8),
    sec_id VARCHAR(8),
    semester VARCHAR(8),
    year NUMERIC(4,0),
    grade VARCHAR(2),
    PRIMARY KEY (ID, course_id, sec_id, semester, year),
    FOREIGN KEY (ID) REFERENCES student(ID),
    FOREIGN KEY (course_id, sec_id, semester, year) REFERENCES teaches(course_id, sec_id, semester, year)
);
```
### 7. 教室表（section）- P25-P27 集合操作示例表
```sql
CREATE TABLE section (
    course_id VARCHAR(8),
    sec_id VARCHAR(8),
    semester VARCHAR(8),
    year NUMERIC(4,0),
    building VARCHAR(20),
    room_number VARCHAR(8),
    time_slot_id VARCHAR(8),
    PRIMARY KEY (course_id, sec_id, semester, year),
    FOREIGN KEY (course_id) REFERENCES course(course_id)
);
```
### Python执行表定义
```python
# 依次执行上述建表SQL
sql_list = [
    # 复制上述7个CREATE TABLE语句
]
for sql in sql_list:
    execute_sql(sql, "创建表")
```

---

# 三、插入测试数据（DML）- P45、P90
**P45/P90**：`INSERT INTO`插入元组，以下插入讲义P4的`instructor`示例数据和关联基础数据，保证后续查询可运行。
### 1. 插入系部数据
```sql
INSERT INTO department VALUES 
('Comp. Sci.', 'Watson', 100000),
('Finance', 'Painter', 80000),
('Music', 'Packard', 50000),
('Physics', 'Watson', 90000),
('History', 'Painter', 60000),
('Biology', 'Watson', 70000),
('Elec. Eng.', 'Taylor', 85000);
```
### 2. 插入讲师数据（P4 完整示例）
```sql
INSERT INTO instructor VALUES 
('10101', 'Srinivasan', 'Comp. Sci.', 65000),
('12121', 'Wu', 'Finance', 90000),
('15151', 'Mozart', 'Music', 40000),
('22222', 'Einstein', 'Physics', 95000),
('32343', 'El Said', 'History', 60000),
('33456', 'Gold', 'Physics', 87000),
('45565', 'Katz', 'Comp. Sci.', 75000),
('58583', 'Califieri', 'History', 62000),
('76543', 'Singh', 'Finance', 80000),
('76766', 'Crick', 'Biology', 72000),
('83821', 'Brandt', 'Comp. Sci.', 92000),
('98345', 'Kim', 'Elec. Eng.', 80000);
```
### 3. 插入课程/授课数据（联表查询用）
```sql
-- 课程表
INSERT INTO course VALUES 
('CS-101', 'Intro to CS', 'Comp. Sci.', 4),
('CS-315', 'DB Design', 'Comp. Sci.', 3),
('PHY-101', 'Physics 1', 'Physics', 4),
('FIN-201', 'Finance 1', 'Finance', 3),
('MU-199', 'Music Theory', 'Music', 2);

-- 授课表
INSERT INTO teaches VALUES 
('10101', 'CS-101', '1', 'Fall', 2017),
('10101', 'CS-315', '1', 'Spring', 2018),
('22222', 'PHY-101', '1', 'Fall', 2017),
('12121', 'FIN-201', '1', 'Spring', 2018),
('15151', 'MU-199', '1', 'Spring', 2018);

-- 教室表（集合操作示例P25）
INSERT INTO section VALUES 
('CS-101', '1', 'Fall', 2017, 'Watson', '101', 'A'),
('CS-315', '1', 'Spring', 2018, 'Watson', '102', 'B'),
('PHY-101', '1', 'Fall', 2017, 'Watson', '201', 'C'),
('FIN-201', '1', 'Spring', 2018, 'Painter', '301', 'D'),
('CS-101', '1', 'Spring', 2018, 'Watson', '101', 'A');
```
### Python执行数据插入
```python
# 复制上述INSERT语句到sql_list
insert_sql_list = [
    # 插入系部/讲师/课程/授课/教室数据
]
for sql in insert_sql_list:
    execute_sql(sql, "插入测试数据")
```

---

# 四、基础查询（SELECT）- P46-P59
**核心**：SQL基本结构`SELECT *|列 FROM 表 WHERE 条件`，对应关系代数的投影(π)/选择(σ)，P46-P59所有示例。
## 4.1 简单投影查询 - P46-P49
### P47：查询所有讲师姓名
```sql
SELECT name FROM instructor;
```
### P48：查询讲师所属系部（去重）
```sql
SELECT DISTINCT dept_name FROM instructor;
```
### P49：查询讲师ID、姓名、月工资（算术运算+重命名）
```sql
SELECT ID, name, salary/12 AS monthly_salary FROM instructor;
```
### P49：查询所有讲师信息（通配符*）
```sql
SELECT * FROM instructor;
```

## 4.2 条件筛选查询（WHERE）- P50-P51
### P50：查询计算机系（Comp. Sci.）的讲师
```sql
SELECT name FROM instructor WHERE dept_name = 'Comp. Sci.';
```
### P51：查询计算机系工资>70000的讲师
```sql
SELECT name FROM instructor WHERE dept_name = 'Comp. Sci.' AND salary > 70000;
```
### P59：查询工资在90000-100000之间的讲师（BETWEEN）
```sql
SELECT name FROM instructor WHERE salary BETWEEN 90000 AND 100000;
```

## 4.3 字符串匹配（LIKE）- P56-P57
### P56：查询姓名包含「dar」的讲师（通配符%）
```sql
SELECT name FROM instructor WHERE name LIKE '%dar%';
```
### 匹配姓名以「S」开头的讲师
```sql
SELECT name FROM instructor WHERE name LIKE 'S%';
```

## 4.4 结果排序（ORDER BY）- P58
### P58：按姓名升序排列讲师
```sql
SELECT DISTINCT name FROM instructor ORDER BY name ASC;
```
### 按系部升序、工资降序排列
```sql
SELECT name, dept_name, salary FROM instructor ORDER BY dept_name, salary DESC;
```

## 4.5 多表联查（笛卡尔积+WHERE）- P52-P53、P20-P23
### P53：查询讲师姓名及所授课程ID（instructor+teaches联表）
```sql
SELECT i.name, t.course_id 
FROM instructor i, teaches t 
WHERE i.ID = t.ID;  -- 等值连接，对应关系代数的JOIN
```

---

# 五、高级查询 - P60-P86
## 5.1 集合操作（UNION/INTERSECT/EXCEPT）- P60-P61、P25-P28
**P25-P28**：基于`section`表，查询不同学期开设的课程，要求两表**同结构**。
### P60：查询2017年秋季OR2018年春季开设的课程（UNION）
```sql
(SELECT course_id FROM section WHERE semester = 'Fall' AND year = 2017)
UNION
(SELECT course_id FROM section WHERE semester = 'Spring' AND year = 2018);
```
### P60：查询2017年秋季AND2018年春季开设的课程（INTERSECT）
```sql
(SELECT course_id FROM section WHERE semester = 'Fall' AND year = 2017)
INTERSECT
(SELECT course_id FROM section WHERE semester = 'Spring' AND year = 2018);
```
### P60：查询2017年秋季开设但2018年春季未开设的课程（EXCEPT）
```sql
(SELECT course_id FROM section WHERE semester = 'Fall' AND year = 2017)
EXCEPT
(SELECT course_id FROM section WHERE semester = 'Spring' AND year = 2018);
```

## 5.2 聚合函数（AVG/MIN/MAX/SUM/COUNT）- P64-P67
**P64-P67**：对列进行统计计算，结合`GROUP BY`分组、`HAVING`分组筛选。
### P65：查询计算机系讲师的平均工资
```sql
SELECT AVG(salary) AS avg_salary FROM instructor WHERE dept_name = 'Comp. Sci.';
```
### P65：查询授课表中2018年春季授课的讲师人数（去重COUNT）
```sql
SELECT COUNT(DISTINCT ID) FROM teaches WHERE semester = 'Spring' AND year = 2018;
```
### P66：查询各系部的平均工资（GROUP BY）
```sql
SELECT dept_name, AVG(salary) AS avg_salary FROM instructor GROUP BY dept_name;
```
### P68：查询平均工资>42000的系部（GROUP BY + HAVING）
```sql
SELECT dept_name, AVG(salary) AS avg_salary 
FROM instructor 
GROUP BY dept_name 
HAVING AVG(salary) > 42000;
```
**关键**：`WHERE`筛选原始行，`HAVING`筛选分组结果（P68）。

## 5.3 嵌套子查询 - P69-P86
### 5.3.1 集合成员判断（IN/NOT IN）- P70-P71
#### P71：查询2017年秋季和2018年春季都开设的课程（IN）
```sql
SELECT DISTINCT course_id 
FROM section 
WHERE semester = 'Fall' AND year = 2017 
AND course_id IN (SELECT course_id FROM section WHERE semester = 'Spring' AND year = 2018);
```
#### 查询姓名既不是Mozart也不是Einstein的讲师（NOT IN）
```sql
SELECT name FROM instructor WHERE name NOT IN ('Mozart', 'Einstein');
```

### 5.3.2 集合比较（SOME/ALL）- P73-P77
#### P74：查询工资高于生物系至少一位讲师的讲师（> SOME）
```sql
SELECT name FROM instructor WHERE salary > SOME (SELECT salary FROM instructor WHERE dept_name = 'Biology');
```
#### P76：查询工资高于生物系所有讲师的讲师（> ALL）
```sql
SELECT name FROM instructor WHERE salary > ALL (SELECT salary FROM instructor WHERE dept_name = 'Biology');
```

### 5.3.3 存在性判断（EXISTS/NOT EXISTS）- P78-P80
#### P79：查询2017年秋季和2018年春季都开设的课程（EXISTS 关联子查询）
```sql
SELECT S.course_id 
FROM section S 
WHERE S.semester = 'Fall' AND S.year = 2017 
AND EXISTS (SELECT * FROM section T WHERE T.semester = 'Spring' AND T.year = 2018 AND S.course_id = T.course_id);
```

### 5.3.4 FROM子句中的子查询 - P82-P83
#### P83：查询平均工资>42000的系部（子查询作为临时表）
```sql
SELECT dept_name, avg_salary 
FROM (SELECT dept_name, AVG(salary) AS avg_salary FROM instructor GROUP BY dept_name) AS dept_avg 
WHERE avg_salary > 42000;
```

### 5.3.5 WITH子句（临时关系）- P84-P85
#### P84：查询预算最高的系部
```sql
WITH max_budget (value) AS (SELECT MAX(budget) FROM department)
SELECT dept_name FROM department, max_budget WHERE department.budget = max_budget.value;
```

---

# 六、数据库修改（INSERT/DELETE/UPDATE）- P45、P87-P94
**P87-P94**：DML三大修改操作，包含条件修改、子查询修改、CASE条件更新。
## 6.1 插入数据（INSERT）- P45、P90
### P90：插入新讲师
```sql
INSERT INTO instructor VALUES ('10211', 'Smith', 'Biology', 66000);
```
### 插入指定列（未指定列设为NULL）
```sql
INSERT INTO instructor (ID, name) VALUES ('10212', 'Johnson');
```

## 6.2 删除数据（DELETE）- P88-P89
### P88：删除金融系（Finance）的讲师
```sql
DELETE FROM instructor WHERE dept_name = 'Finance';
```
### P89：删除工资低于所有讲师平均工资的讲师（子查询）
```sql
DELETE FROM instructor WHERE salary < (SELECT AVG(salary) FROM instructor);
```

## 6.3 更新数据（UPDATE）- P91-P94
### P91：所有讲师工资涨5%
```sql
UPDATE instructor SET salary = salary * 1.05;
```
### P91：工资<70000的讲师涨5%
```sql
UPDATE instructor SET salary = salary * 1.05 WHERE salary < 70000;
```
### P94：CASE条件更新（工资≤100000涨5%，>100000涨3%）
```sql
UPDATE instructor 
SET salary = CASE 
    WHEN salary <= 100000 THEN salary * 1.05 
    ELSE salary * 1.03 
END;
```

---

# 七、Python串联执行所有SQL（完整代码）
将上述所有SQL按**建表→插数据→查询→修改**的顺序放入Python代码，可直接运行，完整代码如下：
```python
import sqlite3

# 1. 连接数据库
conn = sqlite3.connect('univdb-sqlite.db')
cursor = conn.cursor()

# 2. 执行SQL工具函数
def execute_sql(sql, desc=""):
    print(f"\n===== {desc} =====")
    try:
        if sql.strip().upper().startswith("SELECT"):
            cursor.execute(sql)
            res = cursor.fetchall()
            for row in res:
                print(row)
        else:
            cursor.execute(sql)
            conn.commit()
            print("执行成功")
    except Exception as e:
        print(f"执行失败: {e}")

# 3. 建表SQL（DDL）
create_sql = [
    """CREATE TABLE department (dept_name VARCHAR(20) PRIMARY KEY, building VARCHAR(20), budget NUMERIC(10,2));""",
    """CREATE TABLE instructor (ID CHAR(5) PRIMARY KEY, name VARCHAR(20) NOT NULL, dept_name VARCHAR(20), salary NUMERIC(8,2), FOREIGN KEY (dept_name) REFERENCES department(dept_name));""",
    """CREATE TABLE course (course_id VARCHAR(8) PRIMARY KEY, title VARCHAR(50) NOT NULL, dept_name VARCHAR(20), credits NUMERIC(2,0), FOREIGN KEY (dept_name) REFERENCES department(dept_name));""",
    """CREATE TABLE teaches (ID CHAR(5), course_id VARCHAR(8), sec_id VARCHAR(8), semester VARCHAR(8), year NUMERIC(4,0), PRIMARY KEY (ID, course_id, sec_id, semester, year), FOREIGN KEY (ID) REFERENCES instructor(ID), FOREIGN KEY (course_id) REFERENCES course(course_id));""",
    """CREATE TABLE section (course_id VARCHAR(8), sec_id VARCHAR(8), semester VARCHAR(8), year NUMERIC(4,0), building VARCHAR(20), room_number VARCHAR(8), time_slot_id VARCHAR(8), PRIMARY KEY (course_id, sec_id, semester, year), FOREIGN KEY (course_id) REFERENCES course(course_id));"""
]

# 4. 插入数据SQL（DML）
insert_sql = [
    """INSERT INTO department VALUES ('Comp. Sci.', 'Watson', 100000),('Finance', 'Painter', 80000),('Music', 'Packard', 50000),('Physics', 'Watson', 90000),('History', 'Painter', 60000),('Biology', 'Watson', 70000),('Elec. Eng.', 'Taylor', 85000);""",
    """INSERT INTO instructor VALUES ('10101', 'Srinivasan', 'Comp. Sci.', 65000),('12121', 'Wu', 'Finance', 90000),('15151', 'Mozart', 'Music', 40000),('22222', 'Einstein', 'Physics', 95000),('32343', 'El Said', 'History', 60000),('33456', 'Gold', 'Physics', 87000),('45565', 'Katz', 'Comp. Sci.', 75000),('58583', 'Califieri', 'History', 62000),('76543', 'Singh', 'Finance', 80000),('76766', 'Crick', 'Biology', 72000),('83821', 'Brandt', 'Comp. Sci.', 92000),('98345', 'Kim', 'Elec. Eng.', 80000);""",
    """INSERT INTO course VALUES ('CS-101', 'Intro to CS', 'Comp. Sci.', 4),('CS-315', 'DB Design', 'Comp. Sci.', 3),('PHY-101', 'Physics 1', 'Physics', 4),('FIN-201', 'Finance 1', 'Finance', 3),('MU-199', 'Music Theory', 'Music', 2);""",
    """INSERT INTO teaches VALUES ('10101', 'CS-101', '1', 'Fall', 2017),('10101', 'CS-315', '1', 'Spring', 2018),('22222', 'PHY-101', '1', 'Fall', 2017),('12121', 'FIN-201', '1', 'Spring', 2018),('15151', 'MU-199', '1', 'Spring', 2018);""",
    """INSERT INTO section VALUES ('CS-101', '1', 'Fall', 2017, 'Watson', '101', 'A'),('CS-315', '1', 'Spring', 2018, 'Watson', '102', 'B'),('PHY-101', '1', 'Fall', 2017, 'Watson', '201', 'C'),('FIN-201', '1', 'Spring', 2018, 'Painter', '301', 'D'),('CS-101', '1', 'Spring', 2018, 'Watson', '101', 'A');"""
]

# 5. 核心查询SQL（按需添加）
query_sql = [
    ("SELECT name FROM instructor WHERE dept_name = 'Comp. Sci.' AND salary > 70000;", "P51 计算机系工资>70000的讲师"),
    ("SELECT dept_name, AVG(salary) AS avg_salary FROM instructor GROUP BY dept_name;", "P66 各系部平均工资"),
    ("(SELECT course_id FROM section WHERE semester = 'Fall' AND year = 2017) UNION (SELECT course_id FROM section WHERE semester = 'Spring' AND year = 2018);", "P60 2017秋或2018春开设的课程")
]

# 6. 执行流程
if __name__ == "__main__":
    # 检查表是否存在
    cursor.execute("SELECT name FROM sqlite_master WHERE type='table' AND name='instructor';")
    if cursor.fetchone():
        print("表已存在，跳过建表和插入数据步骤，直接执行查询...")
    else:
        # 建表
        for sql in create_sql:
            execute_sql(sql, "创建表")
        # 插数据
        for sql in insert_sql:
            execute_sql(sql, "插入测试数据")
    
    # 执行查询
    for sql, desc in query_sql:
        execute_sql(sql, desc)
    # 关闭连接
    conn.close()
    print("\n数据库连接关闭")
```

---

# 八、运行说明
1. **环境**：无需额外安装，Python自带sqlite3，直接运行.py文件即可；
2. **数据库文件**：代码默认连接当前目录下的`univdb-sqlite.db`。如果该文件存在且包含表结构，代码将自动跳过建表和插入数据步骤，直接执行查询；
3. **扩展**：可在`query_sql`中添加任意上述讲义中的SQL语句，通过`execute_sql`执行；
4. **兼容**：若使用MariaDB/PostgreSQL，仅需修改Python的数据库连接代码（替换sqlite3为pymysql/psycopg2），SQL语句无需修改。

所有SQL语句均严格对应PDF讲义的页码和示例，覆盖考试核心考点（关系代数、SQL查询、聚合、子查询、数据库修改）。